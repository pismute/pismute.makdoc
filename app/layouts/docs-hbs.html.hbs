{{{contents}}}

<script defer>

require(['$', 'handlebars'], function($, handlebars){
    $('.lang-hbs').each(function(index){
        var data = window.data || $(this).attr('data') || {};
        var src = $(this).text();
        var template = Handlebars.compile(src);
        $(this).text(template(data));
    });
});

</script>
